/**
 * @file
 * Contains implementation of grid config.
 */

// Main size of grid item.
export const mainSize = 300

// Timeout for emulation request to grid data.
export const gridTimeout = 500

// Quantity of generated grid items per one request.
export const oneQueueGeneratedItems = 60
